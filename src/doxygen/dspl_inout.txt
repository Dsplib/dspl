
#ifdef DOXYGEN_EN

/*
* Get DSPL version.
* This function return DSPL version
* ------------------------------------------------------------------------------------------
* Parameters:
*	[in] 	int printFlag -	Print DSPL version to console. 
*
* ------------------------------------------------------------------------------------------
* Author:
*	Sergey Bakhurin. 														www.dsplib.org	
*
*/
DSPL_API int dspl_get_version(int printFlag)


/*!
	\ingroup IN_OUT_GROUP
	\fn  int dspl_get_version(int printFlag)
	\brief Get DSPL version.

	
	This function return DSPL version
	
	\param[in]	printFlag	Print DSPL version to console. 
							DSPL version will be printed if `printFlag` is 1. 
		
	
	\return
	DSPL version
	
	\author
		Sergey Bakhurin.                                                         
		www.dsplib.org    
*/



/*!
	\ingroup IN_OUT_GROUP
	\fn	int dspl_savetxt(double* x, double *y, int n, char* fn)
	\brief Save data to text file.
 
	This function saves a real or complex vector to the txt file `fn`. <BR>
	Result file has format<BR>
	x[0]	y[0]<BR>
	x[1]	y[1]<BR>
	...<BR>
	x[n-1]	y[n-1]<BR>
	
	This file can be used for plotting data by external programs.


	\param[in] x	First array pointer. Vector size is `[n x 1]`.
 
	\param[in] y	Second array pointer. Vector size is `[n x 1]`. Can be `NULL`. 
					Text file will contain only x vector if `y == NULL`

	\param[in] n	Input vector size

	\param[in] fn 	File name
 
	\author
	Sergey Bakhurin. 	www.dsplib.org	
	
	\return
	DSPL_OK if file has been saved successfully.
	DSPL_ERROR_PTR if `x==NULL`.
	DSPL_ERROR_SIZE if `n<1`.
	DSPL_ERROR_FNAME if `fn==NULL`.
	DSPL_ERROR_FOPEN if file `fn` cannot be created.
	
	\note 
	This function rounds data for saving. 
	So this function cannot be used for accuracy verification

*/


#elseif DOXYGEN_RU
/*!
	\ingroup IN_OUT_GROUP
	\fn  int dspl_get_version(int printFlag)
	\brief Функция возвращает версию библиотеки DSPL
    
	\param[in]	printFlag	Флаг вывода текущей версии в консоль. 
							Если `printFlag==1`, тогда функция выведет теущую версию библиотеки в консоль  
		
	
	\return
	Верия библиотеки DSPL в формате 0x00YYMMDD, где YY - год, MM - месяц и DD - день компиляции библиотеки.
	Например 0x000F0B16 соответствует версии 15.11.24 
	
	\author
		Бахурин Сергей.                                                         
		www.dsplib.org    
*/



/*!
	\ingroup IN_OUT_GROUP
	\fn	int dspl_savetxt(double* x, double *y, int n, char* fn)
	\brief Сохранить вектор данных в текстовый файл
 
	Функция сохраняет реальный или комплексный вектор в текстовый файл `fn`. <BR>
	Файл имеет следующий формат<BR>
	x[0]	y[0]<BR>
	x[1]	y[1]<BR>
	...<BR>
	x[n-1]	y[n-1]<BR>
	
	Файл может быть использован для построения графика сторонней программой

	\param[in] x	Указатель на первый вектор. Размер вектора `[n x 1]`.
 
	\param[in] y	Указатель на второй вектор. Размер вектора  `[n x 1]`. Может быть `NULL`. 
					Файл будет содержать только один столбец соответствующий вектору `x` если `y == NULL`

	\param[in] n	Размер входных векторов

	\param[in] fn 	Имя файла
 
	\author
	Бахурин Сергей.
 	www.dsplib.org	
	
	\return
	DSPL_OK Файл сохранен успешно.<BR>
	DSPL_ERROR_PTR если `x==NULL`.<BR>
	DSPL_ERROR_SIZE если `n<1`.<BR>
	DSPL_ERROR_FNAME если `fn==NULL`.<BR>
	DSPL_ERROR_FOPEN если файл `fn` не может быть создан.<BR>
	
	\note 
	Данная функция производит округление данных при записи в файл. Поэтому не рекомендуется использовать ее для верификации данных DSPL.

*/


#endif

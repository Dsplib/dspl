#ifdef DOXYGEN_EN
/*!
	\ingroup FILTER_CONV_GROUP
	\fn int dspl_conv(double* a, int na, double* b,  int nb, double *c)
	\brief Linear convolution for real input vectors.
	
	This function calculates a linear convolution of two real vectors. 
	
	
	\param[in] 	a		First input vector pointer. 
						Vector size is `[na x 1]`. 
		
	\param[in]	na		First vector length.
		
	\param[in] 	b		Second input vector pointer. 
						Vector size is `[nb x 1]`. 
		
	\param[in]	nb		Second vector length.
																
	\param[out]	c		Linear convolution vector real  part pointer. 
						Vector size is `[na + nb - 1  x  1]`.	
						Memory must be allocated.
																		
	\return
	`DSPL_OK` 			if convolution is calculated successfully. <br>
	
	`DSPL_ERROR_PTR`	if pointer are wrong. This situation can be if
						`a` is `NULL` or `b` is `NULL` or `c` is `NULL`.<br>
	
	`DSPL_ERROR_SIZE`	if `na<1` or `nb < 1`.<br>
	
	\author
		Sergey Bakhurin. 
		www.dsplib.org	
	
*/

/*!
	\ingroup FILTER_CONV_GROUP
	\fn int dspl_conv_cmplx(double* aR, double* aI, int na, 
							double* bR, double* bI, int nb,
							double *cR, double *cI)
	\brief Linear convolution for real input vectors.

	 This function calculates a linear convolution of two real vectors \f$ c = a * b\f$. 


	\param[in] 	aR		First input vector real  part pointer. 
						Vector size is `[na x 1]`. 
			
	\param[in] 	aI		First input vector image part pointer. 
						Vector size is `[na x 1]`.
						This pointer can be NULL if `a` is real vector
			
	\param[in]	na		First vector length.
			
	\param[in] 	bR		Second input vector real  part pointer. 
						Vector size is `[nb x 1]`. 
			
	\param[in] 	bI		Second input vector image part pointer. 
						Vector size is `[nb x 1]`.
						This pointer can be NULL if `b` is real vector
			
	\param[in]	nb		Second vector length.
																	
	\param[out]	cR		Linear convolution vector real  part pointer. 
						Vector size is `[na + nb - 1  x  1]`.	
						Memory must be allocated.
			
	\param[out]	cI		Linear convolution vector real  part pointer. 
						Vector size is `[na + nb - 1  x  1]`.	
						Memory must be allocated.
						This pointer can be `NULL` if `aI` and `bI` are `NULL` pointers
																		
	\return
	`DSPL_OK` 			if convolution is calculated successfully

	`DSPL_ERROR_PTR` 	if pointer are wrong. This situation can be if:
						`aR` is `NULL` or `bR` is `NULL` or `cR` is `NULL`.
						`cI` is NULL if `aI` or `bI` isn't `NULL`.

	`DSPL_ERROR_SIZE` 	if `na<1` or `nb < 1`.

	\author
		Sergey Bakhurin. 
		www.dsplib.org	

*/










#elseif DOXYGEN_RU
/*!
	\ingroup FILTER_CONV_GROUP
	\fn int dspl_conv(double* a, int na, double* b,  int nb, double *c)
	\brief Линейная свертка двух вещественных векторов

	Функция рассчитывает линейную свертку двух векторов \f$ c = a * b\f$.


	\param[in]	a		Указатель на первый вектор  \f$a\f$. 
						Размер вектора `[na x 1]`. 
	
	\param[in]	na		Размер первого вектора
	
	\param[in]	b		Указатель на второй вектор \f$b\f$.
						Размер вектора `[nb x 1]`. 
	
	\param[in]	nb		Размер второго вектора
																	
	\param[out]	c		Указатель на вектор свертки \f$ c = a * b\f$.
						Размер вектора `[na + nb - 1  x  1]`. 
						Память должна быть выделена.
																		
	\return
	`DSPL_OK`			если свертка расчитана успешно.<br>

	`DSPL_ERROR_PTR` 	Ошибка указателя. Эта ситуация означает что
						`a == NULL` или `b == NULL` или `c == NULL`.<br>

	`DSPL_ERROR_SIZE` 	Размеры входных векторов некорректные `na<1` или `nb < 1`.<br>

	\author
		Бахурин Сергей
		www.dsplib.org	

*/



/*!
	\ingroup FILTER_CONV_GROUP
	\fn int dspl_conv_cmplx(double* aR, double* aI, int na, 
							double* bR, double* bI, int nb, 
							double *cR, double *cI)
	\brief Линейная свертка двух комплексных векторов

	Функция рассчитывает линейную свертку двух векторов \f$ c = a * b\f$.


	\param[in]	aR	Указатель вектор реальной части первого вектора \f$a\f$. 
					Размер вектора `[na x 1]`. 

	\param[in]	aI	Указатель вектор мнимой части первого вектора \f$a\f$. 
					Размер вектора `[na x 1]`. Указатель может быть `NULL` 
					если первый вектор \f$a\f$ чисто вещественный.
					
					
	\param[in]	na	Размер первого вектора

	\param[in]	bR	Указатель вектор реальной части второго вектора \f$b\f$.
					Размер вектора `[nb x 1]`. 
					
	\param[in]	bI	Указатель вектор мнимой части второго вектора \f$b\f$. 
					Размер вектора `[nb x 1]`. Указатель может быть `NULL` 
					если первый вектор \f$b\f$ чисто вещественный.

	\param[in]	nb	Размер второго вектора
																   
	\param[out]	cR	Указатель на вектор реальной части свертки \f$ c = a * b\f$.
					Размер вектора `[na + nb - 1  x  1]`. 
					Память должна быть выделена.
					
	\param[out]	cI	Указатель на вектор мнимой части свертки \f$ c = a * b\f$.
					Размер вектора `[na + nb - 1  x  1]`. Этот указатель может  
					быть `NULL` только в случае если `aI == NULL` и `bI == NULL` 
					(оба входных вектора чисто вещественные). В противном случае 
					если хотя бы один входной вектор имеет мнимую часть, то память 
					должна быть выделена и указатель не может быть `NULL`.
																		
	\return
		`DSPL_OK`	если свертка расчитана успешно.

		`DSPL_ERROR_PTR` Ошибка указателя. 

		`DSPL_ERROR_SIZE` Размеры входных векторов некорректные `na<1` или `nb < 1`.

	\author
		Бахурин Сергей
		www.dsplib.org	

*/

#endif


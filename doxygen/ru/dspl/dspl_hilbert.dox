/*!
	\ingroup HILBERT_GROUP
	\fn int dspl_hilbert(double* x, int n, void* pdspl, double* y)
			
	\brief Функция преобразования Гильберта
	
	Функция рассчитывает аналитический сигнал с использованием преобразователя Гильберта.
	Преобраазование Гильберта реализуется в частотной области путем обнуления спектральных отсчетов
	отрицательных частот.
		
	\param[in]	x		Указатель на вектор реальной входного вещественного  сигнала.<BR> 
						Размер вектора `[n x 1]`. <BR><BR>
	
	
	\param[in]	 n		Размер вектора входного сигнала.<BR><BR>
	
	\param[in]	pdspl	Указатель на объект DSPL. <BR>
						Объект DSPL это внутренняя структура, которая хранит необходимые данные для алгоритмов библиотеки.<BR>
						Объект DSPL должен быть  однократно заполнен функцией `#dspl_obj_create` после связывания с DSPL, 
						и должен быть очищен функцией `#dspl_obj_free` перед выходом.<BR><BR>
	

	\param[out]	y	 	Указатель на вектор аналитического дополнения сигнала `x`.<BR> 
						Размер вектора `[n x 1]`. <BR>
						Память должна быть выделена заранее.<BR><BR>
	
	
	\return
		`DSPL_OK`  если преобразование рассчитано успешно. <BR>
		В противном случае \ref ERROR_CODE_GROUP "код ошибки":<BR>
		`DSPL_ERROR_PTR`  если `x == NULL` или `y == NULL` или `pdspl == NULL`. <BR>
		`DSPL_ERROR_SIZE` если `n<1`.<BR>
	
	
	
	\author
		Бахурин Сергей.
 		www.dsplib.org	

*/
\documentclass[tikz, border = 3mm]{standalone}
\input{header.htex}
\pgfplotsset{width=20cm, height = 5.5cm}



\begin{document}
\begin{tikzpicture}

%\draw[white] (-2,-1) rectangle (20,7.5);

	\begin{axis}[xshift = 0cm, xlabel={$f_k,$ Гц},
		ylabel={$ \left| S_0(k) \right| $},
		xmin =  -500, 
		xmax =  500,
		ymin =  0,
		ymax =  110,
		grid = both,
		every major grid/.style={black!30, opacity=0.5},
		every minor grid/.style={black!10, opacity=0.5},
		minor tick num=4]
		\addplot + [ycomb,draw=blue, mark = *, mark options={scale=0.5, fill = blue}] table {../dat/win_dtft_conv_s0_freq.txt};	
	\end{axis}

	\begin{axis}[yshift = -6cm, xlabel={$f_k,$ Гц},
		ylabel={$\left| W(k) \right|$},
		xmin =  -500,
		xmax =  500,
		ymin =  0,
		ymax =  60,
		grid = both,
		every major grid/.style={black!30, opacity=0.5},
		every minor grid/.style={black!10, opacity=0.5},
		minor tick num=4]
		\addplot + [ycomb,draw=blue, mark = *, mark options={scale=0.5, fill = blue}] table {../dat/win_dtft_conv_w_freq.txt};	
	\end{axis}
	\begin{axis}[yshift = -12cm, xlabel={$f_k,$ Гц},
		ylabel={$\left| Z_0(k)\right|$ },
		xmin =  -500,
		xmax =  500,
		ymin =  0,
		ymax =  28,
		grid = both,
		every major grid/.style={black!30, opacity=0.5},
		every minor grid/.style={black!10, opacity=0.5},
		minor tick num=4]
		\addplot + [ycomb,draw=blue, mark = *, mark options={scale=0.5, fill = blue}] table {../dat/win_dtft_conv_z0_freq.txt};	
	\end{axis}	
		
	
	
%\node[] at (4.2, -7.5) {а)};
%\node[] at (15.2, -7.5) {б)};
	
	
	
	
	
	
	\end{tikzpicture}
\end{document}
\documentclass[tikz, border = 3mm]{standalone}
\input{header.htex}
\pgfplotsset{width=20cm, height = 5.5cm}



\begin{document}
\begin{tikzpicture}


%\draw[white] (-2,-1) rectangle (20,7.5);

	\begin{axis}[xshift = 0cm, xlabel={$n$},
		ylabel={$s_0(n)$},
		xmin =  0,
		xmax =  200,
		ymin = -1.1,
		ymax =  1.1,
		grid = both,
		every major grid/.style={black!30, opacity=0.5},
		every minor grid/.style={black!10, opacity=0.5},
		minor tick num=3]
		\addplot + [ycomb,draw=blue, mark = *, mark options={scale=0.5, fill = blue}] table {../dat/win_dtft_conv_s0_time.txt};	
	\end{axis}

	\begin{axis}[yshift = -6cm, xlabel={$n$},
		ylabel={$w(n)$},
		xmin =  0,
		xmax =  200,
		ymin = -0.1,
		ymax =  1.1,
		grid = both,
		every major grid/.style={black!30, opacity=0.5},
		every minor grid/.style={black!10, opacity=0.5},
		minor tick num=3]
		\addplot + [ycomb,draw=blue, mark = *, mark options={scale=0.5, fill = blue}] table {../dat/win_dtft_conv_w_time.txt};	
	\end{axis}
	\begin{axis}[yshift = -12cm, xlabel={$n$},
		ylabel={$z_0(n)$},
		xmin =  0,
		xmax =  200,
		ymin = -1.1,
		ymax =  1.1,
		grid = both,
		every major grid/.style={black!30, opacity=0.5},
		every minor grid/.style={black!10, opacity=0.5},
		minor tick num=3]
		\addplot + [ycomb,draw=blue, mark = *, mark options={scale=0.5, fill = blue}] table {../dat/win_dtft_conv_z0_time.txt};	
	\end{axis}	
		
	
	
%\node[] at (4.2, -7.5) {а)};
%\node[] at (15.2, -7.5) {б)};
	
	
	
	
	
	
	\end{tikzpicture}
\end{document}